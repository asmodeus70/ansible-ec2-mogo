---
dependencies:
  - { role: deploy-prompts, deploy_prompts: [aws_region, mfa_token] }
  - platform-vars
  - boto

galaxy_info:
  author: cpcundill
  description: Installs a daemon which updates a Route 53 record set with the instance IP address
  company: Driver and Vehicle Licensing Agency (DVLA)
  license: license (MIT)
  min_ansible_version: 2.0
  platforms:
    - name: Amazon
      versions:
        - all
  galaxy_tags:
    - route53
    - networking
    - dns